# Production Environment Variables for Vercel Backend
# ⚠️ Add these as Environment Variables in Vercel Dashboard (Settings > Environment Variables)

NODE_ENV=production

# MongoDB Atlas Connection (REQUIRED)
DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/mutual_funds_db?retryWrites=true&w=majority

# JWT Secret (REQUIRED - Generate a strong random secret)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Google OAuth (REQUIRED for Google Sign-In)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Frontend URL for CORS
FRONTEND_URL=https://mf-frontend-coral.vercel.app

# Email Service (Optional - for magic link auth)
RESEND_API_KEY=your-resend-api-key

# NewsData API (Optional - for news feature)
NEWSDATA_API_KEY=your-newsdata-api-key

# Redis/Upstash (Optional - for caching and queues)
REDIS_URL=redis://default:password@host:port

# OpenAI (Optional - for AI features)
OPENAI_API_KEY=your-openai-api-key

# Port (Not used in Vercel serverless)
PORT=3002
